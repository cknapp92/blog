mixin postCard(post = {}) 
  .store
    .store__hero
      .store__actions
        .store__action.store__action--edit
          a(href=`/posts/${post._id}/edit`)
            != h.icon('pencil')
      img(src=`/uploads/${post.photo || 'store.png'}`)
      h2.title
        span.underline
          a(href=`/posts/${post.slug}`) #{post.name}
    .store__details
      p= post.description.split(' ').slice(0, 25).join(' ')

